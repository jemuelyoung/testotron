
/**
 * Does some work.
 * @param {Number} num doesn't do much
 * @return {Number}
 */
function foo(num) {
  return num;
}

/**
 * Does some more work.
 * @param {String} str doesn't do much
 * @return {String}
 */
function foo2(str) {
  return "hello";
}


