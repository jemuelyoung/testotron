/**
 * Mulitplies your number.
 * @param {Number} num The number to triple
 * @param {Number} multiplier The number to triple
 * @return {Number}    The new number.
 */
function multiply(num, multiplier) {
  var newNum = multiplier * num;
  return newNum;
}